{
  "name": "sequencepang2",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "test": "node scripts/build-sw.mjs --check && node --test tests/*.test.mjs",
    "generate:campaign": "node scripts/generate-campaign.mjs",
    "generate:expansion": "node scripts/generate-expansion.mjs",
    "build:pwa": "node scripts/build-sw.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
